name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # UI and Icons
  cupertino_icons: ^1.0.8

  # Firebase dependencies
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0

  # Utilities
  intl: ^0.20.2
  uuid: ^4.5.1
  fl_chart: ^0.68.0
  provider: ^6.1.5+1
  shared_preferences: ^2.5.3
  lottie: ^3.3.2

  # Google Sign-In (single correct version)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/animations/subtrack.json